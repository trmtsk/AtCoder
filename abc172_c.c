#include <stdio.h>

int main()
{
    int n,m,k;
    int a[200000],b[200000];
    int sum=0,max=0;
    scanf("%d %d %d",&n,&m,&k);
    for(int i = 0; i < n; i++) scanf("%d",&a[i]);
    for(int j = 0; j < n; j++) scanf("%d",&b[j]);
    while(sum<k){
        
    }
    return 0;
}
