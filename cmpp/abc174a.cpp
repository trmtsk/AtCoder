#include <bits/stdc++.h>
#define rep(i, n) for(int i = 0; i < (int)(n); i++)
using ll = long long;
using namespace std;
int main(){
    int X(0);
    cin >> X;
    if (X >= 30)
    {
        cout << "Yes" << endl;
    }else{
        cout << "No" << endl;
    }
}
